{
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build": "cd app && npm run build"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "prettier-plugin-svelte": "^3.4.0"
  }
}